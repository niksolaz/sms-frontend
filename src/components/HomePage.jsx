import React, { Component } from 'react';

export default class HomePage extends Component {
    render (){
        return(
            <div>
                <textarea></textarea>
                <button>Send</button>
            </div>
        );
    }
}